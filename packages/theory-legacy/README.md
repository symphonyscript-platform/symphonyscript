# theory

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build theory` to build the library.

## Running unit tests

Run `nx test theory` to execute the unit tests via [Jest](https://jestjs.io).
